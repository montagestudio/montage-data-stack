describe('DataKafkaService', () => {
  beforeEach((done) => {
     done();        
  });
  describe('MainService', () => {
      it('it should load main-kafka-worker.mjson', (done) => {
        done();
      });
  });
});