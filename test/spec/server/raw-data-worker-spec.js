describe('RawDataWorkerService', () => {
  beforeEach((done) => {
     done();        
  });
  describe('MainService', () => {
      it('it should load main-raw-data-worker.mjson', (done) => {
        done();
      });
  });
});